export interface CarState {
  types: string
}

export interface typeReducer {
  typesCar: {
    types: string
  }
}

export interface isLoginReducer {
  isLogin: {
    isLogin: boolean
  }
}

export interface selectCars {
  types: string
  images: string
}
